<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yasith Hasanka | Premium Electronics & Robotics Store</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }
        .mono {
            font-family: 'JetBrains Mono', monospace;
        }
        body {
            background-color: #121212;
            color: #ffffff;
            overflow-x: hidden;
        }
        
        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #1a1a1a;
        }
        ::-webkit-scrollbar-thumb {
            background: #3b82f6;
            border-radius: 4px;
        }
        
        /* Glassmorphism */
        .glass {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }
        .glass-strong {
            background: rgba(30, 30, 30, 0.7);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 1px solid rgba(255, 255, 255, 0.08);
        }
        
        /* Glow Effects */
        .glow-blue {
            box-shadow: 0 0 30px rgba(59, 130, 246, 0.3);
        }
        .glow-cyan {
            box-shadow: 0 0 40px rgba(6, 182, 212, 0.4);
        }
        .text-glow {
            text-shadow: 0 0 20px rgba(59, 130, 246, 0.5);
        }
        
        /* Animations */
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }
        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
        
        @keyframes pulse-glow {
            0%, 100% { opacity: 0.4; }
            50% { opacity: 0.8; }
        }
        .animate-pulse-glow {
            animation: pulse-glow 3s ease-in-out infinite;
        }
        
        @keyframes slide-up {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .slide-up {
            animation: slide-up 0.6s ease-out forwards;
        }
        
        /* Circuit Pattern Background */
        .circuit-bg {
            background-image: 
                linear-gradient(rgba(59, 130, 246, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(59, 130, 246, 0.03) 1px, transparent 1px);
            background-size: 50px 50px;
        }
        
        /* Product Card Hover */
        .product-card {
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(59, 130, 246, 0.2);
        }
        
        /* Category Button */
        .category-btn {
            transition: all 0.3s ease;
        }
        .category-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 0 30px rgba(59, 130, 246, 0.4);
        }
        .category-btn.active {
            background: rgba(59, 130, 246, 0.2);
            border-color: #3b82f6;
            box-shadow: 0 0 30px rgba(59, 130, 246, 0.3);
        }
        
        /* Search Bar Focus */
        .search-focus:focus {
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.3);
        }
        
        /* Cart Badge Animation */
        @keyframes bounce-in {
            0% { transform: scale(0); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }
        .badge-bounce {
            animation: bounce-in 0.3s ease-out;
        }
        
        /* Hero Gradient */
        .hero-gradient {
            background: radial-gradient(ellipse at top right, rgba(59, 130, 246, 0.15), transparent 50%),
                        radial-gradient(ellipse at bottom left, rgba(6, 182, 212, 0.1), transparent 50%);
        }
        
        /* Neon Text */
        .neon-text {
            color: #06b6d4;
            text-shadow: 0 0 10px rgba(6, 182, 212, 0.5), 0 0 20px rgba(6, 182, 212, 0.3);
        }
        
        /* Grid Pattern */
        .grid-pattern {
            background-image: linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
                              linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
            background-size: 100px 100px;
        }
        
        /* Smooth Scroll */
        html {
            scroll-behavior: smooth;
        }
        
        /* Modal Animation */
        .modal-enter {
            animation: modal-in 0.3s ease-out forwards;
        }
        @keyframes modal-in {
            from { opacity: 0; transform: scale(0.9); }
            to { opacity: 1; transform: scale(1); }
        }
        
        /* Loading Skeleton */
        .skeleton {
            background: linear-gradient(90deg, #1a1a1a 25%, #2a2a2a 50%, #1a1a1a 75%);
            background-size: 200% 100%;
            animation: loading 1.5s infinite;
        }
        @keyframes loading {
            0% { background-position: 200% 0; }
            100% { background-position: -200% 0; }
        }
    </style>
</head>
<body class="circuit-bg">

    <!-- Navigation -->
    <nav class="fixed top-0 w-full z-50 glass-strong border-b border-white/5">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-20">
                <!-- Logo -->
                <div class="flex items-center gap-3 cursor-pointer" onclick="window.scrollTo(0,0)">
                    <div class="w-10 h-10 rounded-lg bg-gradient-to-br from-blue-500 to-cyan-400 flex items-center justify-center glow-blue">
                        <i data-lucide="cpu" class="w-6 h-6 text-white"></i>
                    </div>
                    <div>
                        <h1 class="text-xl font-bold tracking-tight text-white">Yasith Hasanka</h1>
                        <p class="text-xs text-gray-400 mono tracking-wider">ELECTRONICS LAB</p>
                    </div>
                </div>

                <!-- Search Bar -->
                <div class="hidden md:flex flex-1 max-w-2xl mx-8">
                    <div class="relative w-full group">
                        <input type="text" 
                               id="searchInput"
                               placeholder="Search by part number (e.g., L298N, HC-05)..." 
                               class="w-full bg-white/5 border border-white/10 rounded-xl px-5 py-3 pl-12 text-sm text-white placeholder-gray-400 focus:outline-none search-focus transition-all duration-300 group-hover:bg-white/10">
                        <i data-lucide="search" class="absolute left-4 top-3.5 w-5 h-5 text-gray-400 group-hover:text-blue-400 transition-colors"></i>
                        <div class="absolute right-3 top-2.5 flex gap-2">
                            <span class="px-2 py-1 bg-white/5 rounded text-xs text-gray-500 mono border border-white/5">⌘K</span>
                        </div>
                    </div>
                </div>

                <!-- Right Actions -->
                <div class="flex items-center gap-4">
                    <button onclick="toggleSupport()" class="hidden sm:flex items-center gap-2 px-4 py-2 rounded-lg hover:bg-white/5 transition-colors text-gray-300 hover:text-white">
                        <i data-lucide="book-open" class="w-5 h-5"></i>
                        <span class="text-sm font-medium">Guides</span>
                    </button>
                    
                    <button onclick="toggleCart()" class="relative p-2 rounded-lg hover:bg-white/5 transition-colors group">
                        <i data-lucide="shopping-cart" class="w-6 h-6 text-gray-300 group-hover:text-blue-400 transition-colors"></i>
                        <span id="cartBadge" class="absolute -top-1 -right-1 w-5 h-5 bg-blue-500 rounded-full text-xs flex items-center justify-center font-bold hidden">0</span>
                    </button>
                    
                    <button class="md:hidden p-2 rounded-lg hover:bg-white/5" onclick="toggleMobileMenu()">
                        <i data-lucide="menu" class="w-6 h-6 text-white"></i>
                    </button>
                </div>
            </div>
        </div>
    </nav>

    <!-- Mobile Menu -->
    <div id="mobileMenu" class="fixed inset-0 z-40 bg-black/95 backdrop-blur-xl hidden flex-col pt-24 px-6">
        <button onclick="toggleMobileMenu()" class="absolute top-6 right-6 p-2">
            <i data-lucide="x" class="w-8 h-8 text-white"></i>
        </button>
        <input type="text" placeholder="Search products..." class="w-full bg-white/5 border border-white/10 rounded-xl px-5 py-4 text-white mb-8">
        <div class="space-y-4 text-center">
            <a href="#products" onclick="toggleMobileMenu()" class="block text-2xl font-light text-white py-4 border-b border-white/10">Products</a>
            <a href="#bundles" onclick="toggleMobileMenu()" class="block text-2xl font-light text-white py-4 border-b border-white/10">Project Bundles</a>
            <a href="#support" onclick="toggleMobileMenu()" class="block text-2xl font-light text-white py-4 border-b border-white/10">Technical Support</a>
        </div>
    </div>

    <!-- Hero Section -->
    <section class="relative min-h-screen flex items-center pt-20 overflow-hidden hero-gradient">
        <!-- Animated Background Elements -->
        <div class="absolute inset-0 overflow-hidden pointer-events-none">
            <div class="absolute top-20 right-10 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl animate-pulse-glow"></div>
            <div class="absolute bottom-20 left-10 w-72 h-72 bg-cyan-500/10 rounded-full blur-3xl animate-pulse-glow" style="animation-delay: 1.5s;"></div>
            <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] border border-white/5 rounded-full"></div>
            <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] border border-white/5 rounded-full"></div>
        </div>

        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
            <div class="grid lg:grid-cols-2 gap-12 items-center">
                <!-- Left Content -->
                <div class="space-y-8 slide-up">
                    <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass border border-blue-500/20 text-blue-400 text-sm font-medium">
                        <span class="w-2 h-2 bg-blue-400 rounded-full animate-pulse"></span>
                        New Arrivals: ESP32 Series Available
                    </div>
                    
                    <h1 class="text-5xl md:text-7xl font-bold leading-tight">
                        Empowering Your <br>
                        <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-cyan-400 text-glow">Innovations</span>
                    </h1>
                    
                    <p class="text-xl text-gray-400 max-w-lg leading-relaxed">
                        Premium Arduino boards, sensors, and robotics components for students, hobbyists, and professionals in Sri Lanka.
                    </p>
                    
                    <div class="flex flex-wrap gap-4">
                        <button onclick="scrollToProducts()" class="px-8 py-4 bg-gradient-to-r from-blue-600 to-blue-500 rounded-xl font-semibold text-white hover:shadow-lg hover:shadow-blue-500/25 transition-all duration-300 transform hover:scale-105 flex items-center gap-2">
                            <span>Explore Store</span>
                            <i data-lucide="arrow-right" class="w-5 h-5"></i>
                        </button>
                        <button onclick="toggleSupport()" class="px-8 py-4 glass rounded-xl font-semibold text-white hover:bg-white/10 transition-all duration-300 flex items-center gap-2 border border-white/10">
                            <i data-lucide="code-2" class="w-5 h-5"></i>
                            <span>View Guides</span>
                        </button>
                    </div>

                    <!-- Stats -->
                    <div class="flex gap-8 pt-8 border-t border-white/10">
                        <div>
                            <div class="text-3xl font-bold text-white">500+</div>
                            <div class="text-sm text-gray-500">Products</div>
                        </div>
                        <div>
                            <div class="text-3xl font-bold text-white">1000+</div>
                            <div class="text-sm text-gray-500">Students Helped</div>
                        </div>
                        <div>
                            <div class="text-3xl font-bold text-white">24h</div>
                            <div class="text-sm text-gray-500">Delivery in Colombo</div>
                        </div>
                    </div>
                </div>

                <!-- Right Content - 3D Visual -->
                <div class="relative animate-float hidden lg:block">
                    <div class="relative z-10">
                        <img src="https://kimi-web-img.moonshot.cn/img/w0.peakpx.com/a2e47d38cbdbf709ce452ec571d4b8fa0b9fa35c.jpg" 
                             alt="Arduino Uno Board" 
                             class="w-full max-w-lg mx-auto rounded-2xl shadow-2xl shadow-blue-500/20 border border-white/10">
                        
                        <!-- Floating Cards -->
                        <div class="absolute -top-4 -right-4 glass p-4 rounded-xl border border-white/10 animate-pulse-glow">
                            <div class="flex items-center gap-3">
                                <div class="w-10 h-10 rounded-lg bg-green-500/20 flex items-center justify-center">
                                    <i data-lucide="check-circle" class="w-5 h-5 text-green-400"></i>
                                </div>
                                <div>
                                    <div class="text-sm font-semibold text-white">In Stock</div>
                                    <div class="text-xs text-gray-400">Arduino Uno R3</div>
                                </div>
                            </div>
                        </div>

                        <div class="absolute -bottom-4 -left-4 glass p-4 rounded-xl border border-white/10">
                            <div class="flex items-center gap-3">
                                <div class="w-10 h-10 rounded-lg bg-blue-500/20 flex items-center justify-center">
                                    <i data-lucide="award" class="w-5 h-5 text-blue-400"></i>
                                </div>
                                <div>
                                    <div class="text-sm font-semibold text-white">Founder's Choice</div>
                                    <div class="text-xs text-gray-400">A/L ICT Recommended</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Decorative Elements -->
                    <div class="absolute inset-0 bg-gradient-to-r from-blue-500/20 to-cyan-500/20 rounded-full blur-3xl -z-10"></div>
                </div>
            </div>
        </div>

        <!-- Scroll Indicator -->
        <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
            <i data-lucide="chevron-down" class="w-6 h-6 text-gray-500"></i>
        </div>
    </section>

    <!-- Categories Section -->
    <section class="py-20 relative">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold text-white mb-4">Browse by Category</h2>
                <p class="text-gray-400">Find exactly what you need for your next project</p>
            </div>
            
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                <button onclick="filterCategory('sensors')" class="category-btn glass p-6 rounded-2xl border border-white/10 text-center group" data-category="sensors">
                    <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-gradient-to-br from-blue-500/20 to-cyan-500/20 flex items-center justify-center group-hover:scale-110 transition-transform">
                        <i data-lucide="activity" class="w-8 h-8 text-blue-400"></i>
                    </div>
                    <h3 class="font-semibold text-white mb-1">Sensors</h3>
                    <p class="text-xs text-gray-500">Ultrasonic, IR, Temp</p>
                </button>

                <button onclick="filterCategory('controllers')" class="category-btn glass p-6 rounded-2xl border border-white/10 text-center group" data-category="controllers">
                    <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-gradient-to-br from-purple-500/20 to-pink-500/20 flex items-center justify-center group-hover:scale-110 transition-transform">
                        <i data-lucide="cpu" class="w-8 h-8 text-purple-400"></i>
                    </div>
                    <h3 class="font-semibold text-white mb-1">Controllers</h3>
                    <p class="text-xs text-gray-500">Arduino, ESP32, RPi</p>
                </button>

                <button onclick="filterCategory('actuators')" class="category-btn glass p-6 rounded-2xl border border-white/10 text-center group" data-category="actuators">
                    <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-gradient-to-br from-orange-500/20 to-red-500/20 flex items-center justify-center group-hover:scale-110 transition-transform">
                        <i data-lucide="cog" class="w-8 h-8 text-orange-400"></i>
                    </div>
                    <h3 class="font-semibold text-white mb-1">Actuators</h3>
                    <p class="text-xs text-gray-500">Motors, Drivers, Servos</p>
                </button>

                <button onclick="filterCategory('power')" class="category-btn glass p-6 rounded-2xl border border-white/10 text-center group" data-category="power">
                    <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-gradient-to-br from-green-500/20 to-emerald-500/20 flex items-center justify-center group-hover:scale-110 transition-transform">
                        <i data-lucide="battery-charging" class="w-8 h-8 text-green-400"></i>
                    </div>
                    <h3 class="font-semibold text-white mb-1">Power</h3>
                    <p class="text-xs text-gray-500">Batteries, Regulators</p>
                </button>
            </div>
        </div>
    </section>

    <!-- Featured Bundles Section -->
    <section id="bundles" class="py-20 relative grid-pattern">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between mb-12">
                <div>
                    <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-cyan-500/10 border border-cyan-500/20 text-cyan-400 text-xs font-semibold mb-4">
                        <i data-lucide="package" class="w-3 h-3"></i>
                        PROJECT BUNDLES
                    </div>
                    <h2 class="text-3xl font-bold text-white">Complete Project Kits</h2>
                    <p class="text-gray-400 mt-2">Everything you need in one package • Save up to 20%</p>
                </div>
                <button class="hidden sm:flex items-center gap-2 text-blue-400 hover:text-blue-300 transition-colors">
                    View All Bundles <i data-lucide="arrow-right" class="w-4 h-4"></i>
                </button>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <!-- Bundle 1 -->
                <div class="glass rounded-2xl overflow-hidden border border-white/10 hover:border-cyan-500/30 transition-all duration-300 group">
                    <div class="relative h-48 overflow-hidden">
                        <div class="absolute inset-0 bg-gradient-to-br from-blue-600/20 to-cyan-600/20"></div>
                        <img src="https://kimi-web-img.moonshot.cn/img/www.ic-components.com/4e7e95a482d938be6c62508e2bf34ab0482ba354.jpg" alt="Bluetooth Car Kit" class="w-full h-full object-cover opacity-80 group-hover:scale-110 transition-transform duration-500">
                        <div class="absolute top-4 right-4 px-3 py-1 bg-cyan-500/20 backdrop-blur-md rounded-full text-cyan-400 text-xs font-bold border border-cyan-500/20">
                            SAVE 15%
                        </div>
                    </div>
                    <div class="p-6">
                        <h3 class="text-xl font-bold text-white mb-2">Bluetooth Car Kit</h3>
                        <p class="text-sm text-gray-400 mb-4">L298N Driver + HC-05 Module + Chassis + Wheels + Jumper Wires</p>
                        <div class="flex items-center justify-between">
                            <div>
                                <span class="text-2xl font-bold text-white">LKR 4,500</span>
                                <span class="text-sm text-gray-500 line-through ml-2">LKR 5,200</span>
                            </div>
                            <button onclick="addToCart('Bluetooth Car Kit', 4500)" class="p-3 bg-blue-600 rounded-xl hover:bg-blue-500 transition-colors">
                                <i data-lucide="plus" class="w-5 h-5 text-white"></i>
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Bundle 2 -->
                <div class="glass rounded-2xl overflow-hidden border border-white/10 hover:border-purple-500/30 transition-all duration-300 group">
                    <div class="relative h-48 overflow-hidden">
                        <div class="absolute inset-0 bg-gradient-to-br from-purple-600/20 to-pink-600/20"></div>
                        <img src="https://kimi-web-img.moonshot.cn/img/img.freepik.com/22fac473f559d71340a82d53cf5b3e566b887a3d.jpg" alt="Smart Home Starter" class="w-full h-full object-cover opacity-80 group-hover:scale-110 transition-transform duration-500">
                        <div class="absolute top-4 right-4 px-3 py-1 bg-purple-500/20 backdrop-blur-md rounded-full text-purple-400 text-xs font-bold border border-purple-500/20">
                            SAVE 20%
                        </div>
                    </div>
                    <div class="p-6">
                        <h3 class="text-xl font-bold text-white mb-2">Smart Home Starter</h3>
                        <p class="text-sm text-gray-400 mb-4">ESP32 + DHT11 + Relay Module + PIR Sensor + Breadboard</p>
                        <div class="flex items-center justify-between">
                            <div>
                                <span class="text-2xl font-bold text-white">LKR 3,800</span>
                                <span class="text-sm text-gray-500 line-through ml-2">LKR 4,750</span>
                            </div>
                            <button onclick="addToCart('Smart Home Starter', 3800)" class="p-3 bg-purple-600 rounded-xl hover:bg-purple-500 transition-colors">
                                <i data-lucide="plus" class="w-5 h-5 text-white"></i>
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Bundle 3 -->
                <div class="glass rounded-2xl overflow-hidden border border-white/10 hover:border-orange-500/30 transition-all duration-300 group">
                    <div class="relative h-48 overflow-hidden">
                        <div class="absolute inset-0 bg-gradient-to-br from-orange-600/20 to-red-600/20"></div>
                        <img src="https://kimi-web-img.moonshot.cn/img/www.electronicwings.com/bade9e791aae03f80a7d90832bb3e799cba6bcc4.jpg" alt="Robotics Arm Set" class="w-full h-full object-cover opacity-80 group-hover:scale-110 transition-transform duration-500">
                        <div class="absolute top-4 right-4 px-3 py-1 bg-orange-500/20 backdrop-blur-md rounded-full text-orange-400 text-xs font-bold border border-orange-500/20">
                            A/L RECOMMENDED
                        </div>
                    </div>
                    <div class="p-6">
                        <h3 class="text-xl font-bold text-white mb-2">Robotics Arm Set</h3>
                        <p class="text-sm text-gray-400 mb-4">4 Servo Motors + PCA9685 + Arduino Nano + Power Supply</p>
                        <div class="flex items-center justify-between">
                            <div>
                                <span class="text-2xl font-bold text-white">LKR 6,200</span>
                                <span class="text-sm text-gray-500 line-through ml-2">LKR 7,100</span>
                            </div>
                            <button onclick="addToCart('Robotics Arm Set', 6200)" class="p-3 bg-orange-600 rounded-xl hover:bg-orange-500 transition-colors">
                                <i data-lucide="plus" class="w-5 h-5 text-white"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Products Grid Section -->
    <section id="products" class="py-20 relative">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex flex-col md:flex-row md:items-center justify-between mb-12 gap-4">
                <div>
                    <h2 class="text-3xl font-bold text-white mb-2">All Components</h2>
                    <p class="text-gray-400">Professional grade electronics for your projects</p>
                </div>
                
                <div class="flex items-center gap-4">
                    <select id="sortSelect" onchange="sortProducts()" class="bg-white/5 border border-white/10 rounded-xl px-4 py-2 text-sm text-white focus:outline-none focus:border-blue-500">
                        <option value="featured">Featured</option>
                        <option value="price-low">Price: Low to High</option>
                        <option value="price-high">Price: High to Low</option>
                        <option value="stock">In Stock First</option>
                    </select>
                    
                    <div class="flex gap-2">
                        <button onclick="setView('grid')" class="p-2 rounded-lg bg-white/10 text-white" id="gridViewBtn">
                            <i data-lucide="grid-3x3" class="w-5 h-5"></i>
                        </button>
                        <button onclick="setView('list')" class="p-2 rounded-lg hover:bg-white/5 text-gray-400" id="listViewBtn">
                            <i data-lucide="list" class="w-5 h-5"></i>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Products Grid -->
            <div id="productsGrid" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
                <!-- Products will be injected here by JavaScript -->
            </div>

            <!-- Load More -->
            <div class="text-center mt-12">
                <button onclick="loadMore()" class="px-8 py-3 glass rounded-xl text-white hover:bg-white/10 transition-all duration-300 border border-white/10 inline-flex items-center gap-2">
                    Load More Products
                    <i data-lucide="chevron-down" class="w-4 h-4"></i>
                </button>
            </div>
        </div>
    </section>

    <!-- Technical Support Section -->
    <section id="support" class="py-20 relative bg-gradient-to-b from-transparent to-blue-900/10">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-16">
                <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass border border-cyan-500/20 text-cyan-400 text-sm font-medium mb-6">
                    <i data-lucide="code-2" class="w-4 h-4"></i>
                    Technical Resources
                </div>
                <h2 class="text-4xl font-bold text-white mb-4">Project Guides & Code Snippets</h2>
                <p class="text-gray-400 max-w-2xl mx-auto">Free resources to help you get started with your A/L ICT and Physics projects</p>
            </div>

            <div class="grid md:grid-cols-3 gap-8">
                <!-- Guide 1 -->
                <div class="glass rounded-2xl p-6 border border-white/10 hover:border-blue-500/30 transition-all duration-300 group cursor-pointer" onclick="showGuide('bluetooth-car')">
                    <div class="w-12 h-12 rounded-xl bg-blue-500/20 flex items-center justify-center mb-4 group-hover:scale-110 transition-transform">
                        <i data-lucide="bluetooth" class="w-6 h-6 text-blue-400"></i>
                    </div>
                    <h3 class="text-lg font-bold text-white mb-2">Bluetooth Controlled Car</h3>
                    <p class="text-sm text-gray-400 mb-4">Complete guide with Arduino code for building your first robot car using HC-05.</p>
                    <div class="flex items-center gap-4 text-xs text-gray-500">
                        <span class="flex items-center gap-1"><i data-lucide="clock" class="w-3 h-3"></i> 30 min read</span>
                        <span class="flex items-center gap-1"><i data-lucide="download" class="w-3 h-3"></i> PDF + Code</span>
                    </div>
                </div>

                <!-- Guide 2 -->
                <div class="glass rounded-2xl p-6 border border-white/10 hover:border-purple-500/30 transition-all duration-300 group cursor-pointer" onclick="showGuide('weather-station')">
                    <div class="w-12 h-12 rounded-xl bg-purple-500/20 flex items-center justify-center mb-4 group-hover:scale-110 transition-transform">
                        <i data-lucide="cloud" class="w-6 h-6 text-purple-400"></i>
                    </div>
                    <h3 class="text-lg font-bold text-white mb-2">IoT Weather Station</h3>
                    <p class="text-sm text-gray-400 mb-4">Build a cloud-connected weather monitor using ESP32 and DHT22 sensor.</p>
                    <div class="flex items-center gap-4 text-xs text-gray-500">
                        <span class="flex items-center gap-1"><i data-lucide="clock" class="w-3 h-3"></i> 45 min read</span>
                        <span class="flex items-center gap-1"><i data-lucide="download" class="w-3 h-3"></i> PDF + Code</span>
                    </div>
                </div>

                <!-- Guide 3 -->
                <div class="glass rounded-2xl p-6 border border-white/10 hover:border-orange-500/30 transition-all duration-300 group cursor-pointer" onclick="showGuide('home-automation')">
                    <div class="w-12 h-12 rounded-xl bg-orange-500/20 flex items-center justify-center mb-4 group-hover:scale-110 transition-transform">
                        <i data-lucide="home" class="w-6 h-6 text-orange-400"></i>
                    </div>
                    <h3 class="text-lg font-bold text-white mb-2">Home Automation</h3>
                    <p class="text-sm text-gray-400 mb-4">A/L ICT Project: Smart light control with PIR sensors and relay modules.</p>
                    <div class="flex items-center gap-4 text-xs text-gray-500">
                        <span class="flex items-center gap-1"><i data-lucide="clock" class="w-3 h-3"></i> 1 hour read</span>
                        <span class="flex items-center gap-1"><i data-lucide="download" class="w-3 h-3"></i> PDF + Code</span>
                    </div>
                </div>
            </div>

            <!-- Code Snippet Preview -->
            <div class="mt-12 glass rounded-2xl p-6 border border-white/10 max-w-3xl mx-auto">
                <div class="flex items-center justify-between mb-4">
                    <div class="flex items-center gap-2">
                        <div class="w-3 h-3 rounded-full bg-red-500"></div>
                        <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
                        <div class="w-3 h-3 rounded-full bg-green-500"></div>
                    </div>
                    <span class="text-xs text-gray-500 mono">example.ino</span>
                </div>
                <pre class="text-sm text-gray-300 mono overflow-x-auto"><code><span class="text-purple-400">void</span> <span class="text-blue-400">setup</span>() {
  <span class="text-gray-500">// Initialize serial communication</span>
  Serial.<span class="text-blue-400">begin</span>(9600);
  pinMode(LED_BUILTIN, OUTPUT);
}

<span class="text-purple-400">void</span> <span class="text-blue-400">loop</span>() {
  <span class="text-purple-400">if</span> (Serial.<span class="text-blue-400">available</span>() > 0) {
    <span class="text-yellow-400">char</span> command = Serial.<span class="text-blue-400">read</span>();
    <span class="text-purple-400">if</span> (command == <span class="text-green-400">'1'</span>) {
      digitalWrite(LED_BUILTIN, HIGH);
    }
  }
}</code></pre>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="border-t border-white/10 pt-20 pb-10 bg-black/50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid md:grid-cols-4 gap-12 mb-12">
                <div>
                    <div class="flex items-center gap-3 mb-6">
                        <div class="w-10 h-10 rounded-lg bg-gradient-to-br from-blue-500 to-cyan-400 flex items-center justify-center">
                            <i data-lucide="cpu" class="w-6 h-6 text-white"></i>
                        </div>
                        <div>
                            <h3 class="text-lg font-bold text-white">Yasith Hasanka</h3>
                            <p class="text-xs text-gray-500 mono">ELECTRONICS LAB</p>
                        </div>
                    </div>
                    <p class="text-gray-400 text-sm leading-relaxed">
                        Empowering Sri Lankan students and makers with premium electronic components since 2020.
                    </p>
                </div>
                
                <div>
                    <h4 class="text-white font-semibold mb-4">Quick Links</h4>
                    <ul class="space-y-2 text-sm text-gray-400">
                        <li><a href="#" class="hover:text-blue-400 transition-colors">New Arrivals</a></li>
                        <li><a href="#" class="hover:text-blue-400 transition-colors">Project Bundles</a></li>
                        <li><a href="#" class="hover:text-blue-400 transition-colors">A/L ICT Kits</a></li>
                        <li><a href="#" class="hover:text-blue-400 transition-colors">Datasheets</a></li>
                    </ul>
                </div>
                
                <div>
                    <h4 class="text-white font-semibold mb-4">Support</h4>
                    <ul class="space-y-2 text-sm text-gray-400">
                        <li><a href="#" class="hover:text-blue-400 transition-colors">Contact Us</a></li>
                        <li><a href="#" class="hover:text-blue-400 transition-colors">Shipping Info</a></li>
                        <li><a href="#" class="hover:text-blue-400 transition-colors">Returns Policy</a></li>
                        <li><a href="#" class="hover:text-blue-400 transition-colors">FAQ</a></li>
                    </ul>
                </div>
                
                <div>
                    <h4 class="text-white font-semibold mb-4">Contact</h4>
                    <ul class="space-y-3 text-sm text-gray-400">
                        <li class="flex items-center gap-2">
                            <i data-lucide="map-pin" class="w-4 h-4 text-blue-400"></i>
                            Colombo, Sri Lanka
                        </li>
                        <li class="flex items-center gap-2">
                            <i data-lucide="phone" class="w-4 h-4 text-blue-400"></i>
                            +94 77 123 4567
                        </li>
                        <li class="flex items-center gap-2">
                            <i data-lucide="mail" class="w-4 h-4 text-blue-400"></i>
                            hello@yasithhasanka.lk
                        </li>
                    </ul>
                    <div class="flex gap-4 mt-6">
                        <a href="#" class="w-10 h-10 rounded-lg glass flex items-center justify-center hover:bg-white/10 transition-colors">
                            <i data-lucide="facebook" class="w-5 h-5 text-gray-400"></i>
                        </a>
                        <a href="#" class="w-10 h-10 rounded-lg glass flex items-center justify-center hover:bg-white/10 transition-colors">
                            <i data-lucide="instagram" class="w-5 h-5 text-gray-400"></i>
                        </a>
                        <a href="#" class="w-10 h-10 rounded-lg glass flex items-center justify-center hover:bg-white/10 transition-colors">
                            <i data-lucide="youtube" class="w-5 h-5 text-gray-400"></i>
                        </a>
                    </div>
                </div>
            </div>
            
            <div class="border-t border-white/10 pt-8 flex flex-col md:flex-row justify-between items-center gap-4">
                <p class="text-sm text-gray-500">© 2024 Yasith Hasanka Electronics. All rights reserved.</p>
                <div class="flex gap-6 text-sm text-gray-500">
                    <a href="#" class="hover:text-white transition-colors">Privacy Policy</a>
                    <a href="#" class="hover:text-white transition-colors">Terms of Service</a>
                </div>
            </div>
        </div>
    </footer>

    <!-- Cart Modal -->
    <div id="cartModal" class="fixed inset-0 z-50 hidden">
        <div class="absolute inset-0 bg-black/80 backdrop-blur-sm" onclick="toggleCart()"></div>
        <div class="absolute right-0 top-0 h-full w-full max-w-md bg-[#1a1a1a] border-l border-white/10 shadow-2xl transform transition-transform duration-300 translate-x-full" id="cartPanel">
            <div class="flex flex-col h-full">
                <div class="flex items-center justify-between p-6 border-b border-white/10">
                    <h2 class="text-xl font-bold text-white flex items-center gap-2">
                        <i data-lucide="shopping-cart" class="w-5 h-5"></i>
                        Your Cart
                    </h2>
                    <button onclick="toggleCart()" class="p-2 hover:bg-white/10 rounded-lg transition-colors">
                        <i data-lucide="x" class="w-6 h-6 text-gray-400"></i>
                    </button>
                </div>
                
                <div id="cartItems" class="flex-1 overflow-y-auto p-6 space-y-4">
                    <div class="text-center text-gray-500 py-12">
                        <i data-lucide="shopping-bag" class="w-16 h-16 mx-auto mb-4 opacity-20"></i>
                        <p>Your cart is empty</p>
                    </div>
                </div>
                
                <div class="border-t border-white/10 p-6 space-y-4">
                    <div class="flex justify-between text-gray-400">
                        <span>Subtotal</span>
                        <span id="cartSubtotal" class="text-white font-semibold">LKR 0</span>
                    </div>
                    <div class="flex justify-between text-gray-400">
                        <span>Shipping</span>
                        <span class="text-green-400">Free</span>
                    </div>
                    <div class="flex justify-between text-xl font-bold text-white pt-4 border-t border-white/10">
                        <span>Total</span>
                        <span id="cartTotal">LKR 0</span>
                    </div>
                    <button onclick="checkout()" class="w-full py-4 bg-gradient-to-r from-blue-600 to-blue-500 rounded-xl font-semibold text-white hover:shadow-lg hover:shadow-blue-500/25 transition-all duration-300 flex items-center justify-center gap-2">
                        Checkout <i data-lucide="arrow-right" class="w-5 h-5"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Product Detail Modal -->
    <div id="productModal" class="fixed inset-0 z-50 hidden flex items-center justify-center p-4">
        <div class="absolute inset-0 bg-black/90 backdrop-blur-sm" onclick="closeProductModal()"></div>
        <div class="relative bg-[#1a1a1a] rounded-2xl max-w-4xl w-full max-h-[90vh] overflow-y-auto border border-white/10 modal-enter">
            <button onclick="closeProductModal()" class="absolute top-4 right-4 p-2 hover:bg-white/10 rounded-lg transition-colors z-10">
                <i data-lucide="x" class="w-6 h-6 text-white"></i>
            </button>
            <div id="productModalContent" class="p-8">
                <!-- Content injected by JS -->
            </div>
        </div>
    </div>

    <!-- Toast Notification -->
    <div id="toast" class="fixed bottom-8 right-8 z-50 transform translate-y-20 opacity-0 transition-all duration-300">
        <div class="glass-strong px-6 py-4 rounded-xl border border-green-500/30 flex items-center gap-3 shadow-2xl">
            <div class="w-10 h-10 rounded-full bg-green-500/20 flex items-center justify-center">
                <i data-lucide="check" class="w-5 h-5 text-green-400"></i>
            </div>
            <div>
                <h4 class="text-white font-semibold">Added to Cart</h4>
                <p class="text-sm text-gray-400" id="toastMessage">Product added successfully</p>
            </div>
        </div>
    </div>

    <script>
        // Initialize Lucide Icons
        lucide.createIcons();

        // Product Data
        const products = [
            {
                id: 1,
                name: "Arduino Uno R3",
                category: "controllers",
                price: 2400,
                stock: 15,
                image: "https://kimi-web-img.moonshot.cn/img/w0.peakpx.com/a2e47d38cbdbf709ce452ec571d4b8fa0b9fa35c.jpg",
                badge: "Founder's Choice",
                badgeColor: "bg-blue-500/20 text-blue-400",
                description: "Original Arduino Uno R3 with ATmega328P. Perfect for A/L ICT projects.",
                datasheet: "arduino-uno-datasheet.pdf"
            },
            {
                id: 2,
                name: "L298N Motor Driver",
                category: "actuators",
                price: 850,
                stock: 8,
                image: "https://kimi-web-img.moonshot.cn/img/www.ic-components.com/4e7e95a482d938be6c62508e2bf34ab0482ba354.jpg",
                badge: "Best Seller",
                badgeColor: "bg-orange-500/20 text-orange-400",
                description: "Dual H-Bridge motor driver for controlling DC motors and stepper motors.",
                datasheet: "l298n-datasheet.pdf"
            },
            {
                id: 3,
                name: "HC-05 Bluetooth Module",
                category: "controllers",
                price: 1200,
                stock: 0,
                image: "https://kimi-web-img.moonshot.cn/img/www.electronicwings.com/bade9e791aae03f80a7d90832bb3e799cba6bcc4.jpg",
                badge: null,
                badgeColor: null,
                description: "Wireless Bluetooth serial transceiver module for Arduino projects.",
                datasheet: "hc05-datasheet.pdf"
            },
            {
                id: 4,
                name: "Ultrasonic Sensor HC-SR04",
                category: "sensors",
                price: 450,
                stock: 25,
                image: "https://kimi-web-img.moonshot.cn/img/www.shutterstock.com/8441612f7b106062e7d54beac98299391dd6724a.jpg",
                badge: null,
                badgeColor: null,
                description: "Distance measuring sensor for obstacle avoidance and ranging.",
                datasheet: "hcsr04-datasheet.pdf"
            },
            {
                id: 5,
                name: "ESP32 DevKit V1",
                category: "controllers",
                price: 1800,
                stock: 12,
                image: "https://kimi-web-img.moonshot.cn/img/img.freepik.com/22fac473f559d71340a82d53cf5b3e566b887a3d.jpg",
                badge: "WiFi + Bluetooth",
                badgeColor: "bg-purple-500/20 text-purple-400",
                description: "Dual-core processor with WiFi and Bluetooth. IoT ready.",
                datasheet: "esp32-datasheet.pdf"
            },
            {
                id: 6,
                name: "Jumper Wires (40pcs)",
                category: "power",
                price: 350,
                stock: 50,
                image: "https://kimi-web-img.moonshot.cn/img/www.az-delivery.de/f2370701820f1e7cd042a192fd776a062fe06749.jpg",
                badge: null,
                badgeColor: null,
                description: "Male-to-male, male-to-female, female-to-female breadboard wires.",
                datasheet: null
            },
            {
                id: 7,
                name: "DHT11 Temperature Sensor",
                category: "sensors",
                price: 280,
                stock: 30,
                image: "https://kimi-web-img.moonshot.cn/img/thumbs.dreamstime.com/943520ef58850e3a2eb745503161d302eeb4e69e.jpg",
                badge: null,
                badgeColor: null,
                description: "Basic temperature and humidity sensor for weather projects.",
                datasheet: "dht11-datasheet.pdf"
            },
            {
                id: 8,
                name: "SG90 Micro Servo",
                category: "actuators",
                price: 320,
                stock: 20,
                image: "https://kimi-web-img.moonshot.cn/img/www.hibit.dev/7296d403865f5a411c29466421b14f8b1710e048.png",
                badge: null,
                badgeColor: null,
                description: "9g micro servo motor for robotics and automation projects.",
                datasheet: "sg90-datasheet.pdf"
            }
        ];

        let cart = [];
        let currentCategory = 'all';
        let currentView = 'grid';

        // Initialize
        document.addEventListener('DOMContentLoaded', () => {
            renderProducts();
            
            // Keyboard shortcut for search
            document.addEventListener('keydown', (e) => {
                if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
                    e.preventDefault();
                    document.getElementById('searchInput').focus();
                }
                if (e.key === 'Escape') {
                    closeProductModal();
                    document.getElementById('cartModal').classList.add('hidden');
                    document.getElementById('mobileMenu').classList.add('hidden');
                }
            });

            // Search functionality
            document.getElementById('searchInput').addEventListener('input', (e) => {
                searchProducts(e.target.value);
            });
        });

        function renderProducts(productsToRender = products) {
            const grid = document.getElementById('productsGrid');
            grid.innerHTML = '';

            const filtered = currentCategory === 'all' 
                ? productsToRender 
                : productsToRender.filter(p => p.category === currentCategory);

            filtered.forEach((product, index) => {
                const card = document.createElement('div');
                card.className = `product-card glass rounded-2xl overflow-hidden border border-white/10 flex ${currentView === 'list' ? 'flex-row' : 'flex-col'}`;
                card.style.animationDelay = `${index * 0.1}s`;
                
                const stockBadge = product.stock > 0 
                    ? `<span class="px-2 py-1 bg-green-500/20 text-green-400 text-xs rounded-full border border-green-500/20">In Stock (${product.stock})</span>`
                    : `<span class="px-2 py-1 bg-red-500/20 text-red-400 text-xs rounded-full border border-red-500/20">Out of Stock</span>`;

                const founderBadge = product.badge 
                    ? `<div class="absolute top-3 left-3 px-3 py-1 ${product.badgeColor} rounded-full text-xs font-bold border border-white/10 backdrop-blur-md">${product.badge}</div>` 
                    : '';

                card.innerHTML = `
                    <div class="relative ${currentView === 'list' ? 'w-48' : 'h-48'} overflow-hidden group cursor-pointer" onclick="showProductDetail(${product.id})">
                        ${founderBadge}
                        <img src="${product.image}" alt="${product.name}" class="w-full h-full object-cover opacity-80 group-hover:scale-110 transition-transform duration-500">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 group-hover:opacity-100 transition-opacity"></div>
                    </div>
                    <div class="p-5 flex-1 flex flex-col">
                        <div class="flex items-start justify-between mb-2">
                            <h3 class="text-lg font-bold text-white cursor-pointer hover:text-blue-400 transition-colors" onclick="showProductDetail(${product.id})">${product.name}</h3>
                        </div>
                        <p class="text-sm text-gray-400 mb-4 line-clamp-2">${product.description}</p>
                        
                        <div class="mt-auto space-y-3">
                            <div class="flex items-center justify-between">
                                <span class="text-xl font-bold text-white">LKR ${product.price.toLocaleString()}</span>
                                ${stockBadge}
                            </div>
                            <div class="flex gap-2">
                                <button onclick="showProductDetail(${product.id})" class="flex-1 py-2 glass rounded-lg text-sm text-white hover:bg-white/10 transition-colors border border-white/10">
                                    Details
                                </button>
                                <button onclick="addToCart('${product.name}', ${product.price}, ${product.id})" 
                                        ${product.stock === 0 ? 'disabled' : ''}
                                        class="flex-1 py-2 bg-blue-600 rounded-lg text-sm text-white hover:bg-blue-500 transition-colors disabled:opacity-50 disabled:cursor-not-allowed flex items-center justify-center gap-1">
                                    <i data-lucide="plus" class="w-4 h-4"></i> Add
                                </button>
                            </div>
                            ${product.datasheet ? `
                            <button onclick="downloadDatasheet('${product.datasheet}')" class="w-full py-2 text-xs text-gray-500 hover:text-blue-400 transition-colors flex items-center justify-center gap-1">
                                <i data-lucide="file-text" class="w-3 h-3"></i> Download Datasheet
                            </button>
                            ` : ''}
                        </div>
                    </div>
                `;
                grid.appendChild(card);
            });
            
            lucide.createIcons();
        }

        function filterCategory(category) {
            currentCategory = category;
            
            // Update UI
            document.querySelectorAll('.category-btn').forEach(btn => {
                if (btn.dataset.category === category) {
                    btn.classList.add('active');
                } else {
                    btn.classList.remove('active');
                }
            });
            
            renderProducts();
        }

        function searchProducts(query) {
            const filtered = products.filter(p => 
                p.name.toLowerCase().includes(query.toLowerCase()) ||
                p.description.toLowerCase().includes(query.toLowerCase()) ||
                p.category.toLowerCase().includes(query.toLowerCase())
            );
            renderProducts(filtered);
        }

        function sortProducts() {
            const sortValue = document.getElementById('sortSelect').value;
            let sorted = [...products];
            
            switch(sortValue) {
                case 'price-low':
                    sorted.sort((a, b) => a.price - b.price);
                    break;
                case 'price-high':
                    sorted.sort((a, b) => b.price - a.price);
                    break;
                case 'stock':
                    sorted.sort((a, b) => b.stock - a.stock);
                    break;
            }
            
            renderProducts(sorted);
        }

        function setView(view) {
            currentView = view;
            document.getElementById('gridViewBtn').className = view === 'grid' ? 'p-2 rounded-lg bg-white/10 text-white' : 'p-2 rounded-lg hover:bg-white/5 text-gray-400';
            document.getElementById('listViewBtn').className = view === 'list' ? 'p-2 rounded-lg bg-white/10 text-white' : 'p-2 rounded-lg hover:bg-white/5 text-gray-400';
            renderProducts();
        }

        function addToCart(name, price, id = null) {
            const existingItem = cart.find(item => item.name === name);
            
            if (existingItem) {
                existingItem.quantity++;
            } else {
                cart.push({ name, price, quantity: 1, id });
            }
            
            updateCartUI();
            showToast(`${name} added to cart`);
            
            // Animate cart icon
            const badge = document.getElementById('cartBadge');
            badge.classList.add('badge-bounce');
            setTimeout(() => badge.classList.remove('badge-bounce'), 300);
        }

        function removeFromCart(index) {
            cart.splice(index, 1);
            updateCartUI();
        }

        function updateQuantity(index, delta) {
            cart[index].quantity += delta;
            if (cart[index].quantity <= 0) {
                removeFromCart(index);
            } else {
                updateCartUI();
            }
        }

        function updateCartUI() {
            const badge = document.getElementById('cartBadge');
            const itemsContainer = document.getElementById('cartItems');
            const subtotalEl = document.getElementById('cartSubtotal');
            const totalEl = document.getElementById('cartTotal');
            
            const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);
            const subtotal = cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
            
            if (totalItems > 0) {
                badge.textContent = totalItems;
                badge.classList.remove('hidden');
            } else {
                badge.classList.add('hidden');
            }
            
            if (cart.length === 0) {
                itemsContainer.innerHTML = `
                    <div class="text-center text-gray-500 py-12">
                        <i data-lucide="shopping-bag" class="w-16 h-16 mx-auto mb-4 opacity-20"></i>
                        <p>Your cart is empty</p>
                        <button onclick="toggleCart(); scrollToProducts()" class="mt-4 text-blue-400 hover:text-blue-300 text-sm">Start Shopping</button>
                    </div>
                `;
            } else {
                itemsContainer.innerHTML = cart.map((item, index) => `
                    <div class="flex gap-4 p-4 glass rounded-xl border border-white/5">
                        <div class="flex-1">
                            <h4 class="text-white font-semibold text-sm">${item.name}</h4>
                            <p class="text-gray-400 text-sm">LKR ${item.price.toLocaleString()}</p>
                        </div>
                        <div class="flex items-center gap-3">
                            <button onclick="updateQuantity(${index}, -1)" class="w-8 h-8 rounded-lg bg-white/5 hover:bg-white/10 flex items-center justify-center text-white transition-colors">
                                <i data-lucide="minus" class="w-4 h-4"></i>
                            </button>
                            <span class="text-white font-semibold w-4 text-center">${item.quantity}</span>
                            <button onclick="updateQuantity(${index}, 1)" class="w-8 h-8 rounded-lg bg-white/5 hover:bg-white/10 flex items-center justify-center text-white transition-colors">
                                <i data-lucide="plus" class="w-4 h-4"></i>
                            </button>
                            <button onclick="removeFromCart(${index})" class="ml-2 p-2 text-gray-500 hover:text-red-400 transition-colors">
                                <i data-lucide="trash-2" class="w-4 h-4"></i>
                            </button>
                        </div>
                    </div>
                `).join('');
            }
            
            subtotalEl.textContent = `LKR ${subtotal.toLocaleString()}`;
            totalEl.textContent = `LKR ${subtotal.toLocaleString()}`;
            lucide.createIcons();
        }

        function toggleCart() {
            const modal = document.getElementById('cartModal');
            const panel = document.getElementById('cartPanel');
            
            if (modal.classList.contains('hidden')) {
                modal.classList.remove('hidden');
                setTimeout(() => {
                    panel.classList.remove('translate-x-full');
                }, 10);
            } else {
                panel.classList.add('translate-x-full');
                setTimeout(() => {
                    modal.classList.add('hidden');
                }, 300);
            }
        }

        function toggleMobileMenu() {
            const menu = document.getElementById('mobileMenu');
            menu.classList.toggle('hidden');
            menu.classList.toggle('flex');
        }

        function toggleSupport() {
            document.getElementById('support').scrollIntoView({ behavior: 'smooth' });
        }

        function scrollToProducts() {
            document.getElementById('products').scrollIntoView({ behavior: 'smooth' });
        }

        function showProductDetail(productId) {
            const product = products.find(p => p.id === productId);
            if (!product) return;
            
            const modal = document.getElementById('productModal');
            const content = document.getElementById('productModalContent');
            
            const stockStatus = product.stock > 0 
                ? `<span class="px-3 py-1 bg-green-500/20 text-green-400 rounded-full text-sm border border-green-500/20"><i data-lucide="check-circle" class="w-4 h-4 inline mr-1"></i> In Stock (${product.stock} units)</span>`
                : `<span class="px-3 py-1 bg-red-500/20 text-red-400 rounded-full text-sm border border-red-500/20"><i data-lucide="x-circle" class="w-4 h-4 inline mr-1"></i> Out of Stock</span>`;
            
            content.innerHTML = `
                <div class="grid md:grid-cols-2 gap-8">
                    <div class="relative rounded-2xl overflow-hidden bg-gradient-to-br from-blue-500/10 to-cyan-500/10">
                        <img src="${product.image}" alt="${product.name}" class="w-full h-full object-cover">
                        ${product.badge ? `<div class="absolute top-4 left-4 px-4 py-2 ${product.badgeColor} rounded-full text-sm font-bold border border-white/10 backdrop-blur-md">${product.badge}</div>` : ''}
                    </div>
                    <div class="space-y-6">
                        <div>
                            <div class="flex items-center gap-3 mb-2">
                                <span class="px-3 py-1 bg-white/5 rounded-full text-xs text-gray-400 uppercase tracking-wider">${product.category}</span>
                                ${stockStatus}
                            </div>
                            <h2 class="text-3xl font-bold text-white mb-2">${product.name}</h2>
                            <p class="text-3xl font-bold text-blue-400">LKR ${product.price.toLocaleString()}</p>
                        </div>
                        
                        <p class="text-gray-400 leading-relaxed">${product.description}</p>
                        
                        <div class="space-y-3">
                            <h4 class="text-white font-semibold flex items-center gap-2">
                                <i data-lucide="file-text" class="w-5 h-5 text-blue-400"></i>
                                Technical Specifications
                            </h4>
                            <ul class="space-y-2 text-sm text-gray-400">
                                <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-green-400"></i> Genuine quality guaranteed</li>
                                <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-green-400"></i> Tested before shipping</li>
                                <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-green-400"></i> 7-day replacement warranty</li>
                                <li class="flex items-center gap-2"><i data-lucide="check" class="w-4 h-4 text-green-400"></i> Technical support included</li>
                            </ul>
                        </div>
                        
                        ${product.datasheet ? `
                        <div class="p-4 glass rounded-xl border border-white/10">
                            <div class="flex items-center justify-between">
                                <div class="flex items-center gap-3">
                                    <div class="w-10 h-10 rounded-lg bg-blue-500/20 flex items-center justify-center">
                                        <i data-lucide="download" class="w-5 h-5 text-blue-400"></i>
                                    </div>
                                    <div>
                                        <p class="text-white font-medium text-sm">Datasheet Available</p>
                                        <p class="text-xs text-gray-500">PDF • 2.4 MB</p>
                                    </div>
                                </div>
                                <button onclick="downloadDatasheet('${product.datasheet}')" class="px-4 py-2 bg-white/5 hover:bg-white/10 rounded-lg text-sm text-white transition-colors border border-white/10">
                                    Download
                                </button>
                            </div>
                        </div>
                        ` : ''}
                        
                        <div class="flex gap-4 pt-4">
                            <button onclick="addToCart('${product.name}', ${product.price}, ${product.id}); closeProductModal()" 
                                    ${product.stock === 0 ? 'disabled' : ''}
                                    class="flex-1 py-4 bg-gradient-to-r from-blue-600 to-blue-500 rounded-xl font-semibold text-white hover:shadow-lg hover:shadow-blue-500/25 transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed flex items-center justify-center gap-2">
                                <i data-lucide="shopping-cart" class="w-5 h-5"></i>
                                Add to Cart
                            </button>
                            <button onclick="closeProductModal()" class="px-6 py-4 glass rounded-xl text-white hover:bg-white/10 transition-colors border border-white/10">
                                <i data-lucide="x" class="w-5 h-5"></i>
                            </button>
                        </div>
                    </div>
                </div>
            `;
            
            modal.classList.remove('hidden');
            lucide.createIcons();
        }

        function closeProductModal() {
            document.getElementById('productModal').classList.add('hidden');
        }

        function showGuide(guideId) {
            showToast('Opening guide: ' + guideId.replace('-', ' '));
            // In a real app, this would open the guide
        }

        function downloadDatasheet(filename) {
            showToast(`Downloading ${filename}...`);
            // In a real app, this would trigger a download
        }

        function checkout() {
            if (cart.length === 0) {
                showToast('Your cart is empty!');
                return;
            }
            showToast('Proceeding to checkout...');
            // In a real app, this would redirect to checkout
        }

        function showToast(message) {
            const toast = document.getElementById('toast');
            document.getElementById('toastMessage').textContent = message;
            
            toast.classList.remove('translate-y-20', 'opacity-0');
            
            setTimeout(() => {
                toast.classList.add('translate-y-20', 'opacity-0');
            }, 3000);
        }

        function loadMore() {
            showToast('Loading more products...');
            // In a real app, this would fetch more products
        }

        // Close modals on escape key
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') {
                closeProductModal();
                const cartModal = document.getElementById('cartModal');
                if (!cartModal.classList.contains('hidden')) {
                    toggleCart();
                }
            }
        });
    </script>
</body>
</html>